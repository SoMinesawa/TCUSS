---
description: このプロジェクトにおけるルール
globs: 
alwaysApply: false
---

# TCUSS
TCUSSとは、私のComputer Vision研究プロジェクト名です。TCUSSは、点群のみを入力とした、Unsupervised Semantic Segmentationの手法であるGrowSPのlossに、運転シーンの3D点群に特化した対照学習手法であるTARLのlossを追加することで、運転シーンの点群に特化したUnsupervised Semantic Segmentation用モデルができるのではないかというもの。つまり、運転シーンの3D点群であるSemanticKITTIデータセットにて、GrowSPよりも高いmIoUがでることが目標です。TARLは、連続する n フレーム分の点群を同じ座標系に集約するという処理を挟んでいるので、自動運転シーンにおいて特に有効である。例えば、停止車両は複数フレームで同じ位置に存在し安定したセグメントとして抽出される一方、移動車両は連続的な軌跡として捉えられ、その動きの文脈も含めたセグメントとして抽出することができるためです。だから精度を上げることができるんじゃないかって考えています。

# ファイル構成
基本的には、GrowSPのプロジェクトディレクトリをそのままコピーして名前を適宜変えて使用しています。

# 現状
<!-- TCUSSのベースラインの実装は終わっていて、テストした結果、ワールド座標系にて移動している物体（車や歩行者など）の精度は向上したが、静止している物体（建物や草木など）の精度は下がってしまった。そのため、静止している物体からのTARLのlossを計算しないようにして、TARL lossは動いている物体のみで計算して、誤差逆伝播したい。 -->

# agentモードの動作の流れ
- 回答に際して、情報収集のために閲覧したいけどどうしても閲覧できないコードがあれば、この旨を私に伝えてください。可能であればそのコードを提示します。
- 「実装して。」と明記されない限り、ファイル操作や編集は絶対に行わないこと。
- 実行した際にエラーが生じたら、原因が明確に1つに絞れるまでデバッグ出力を行います。どこのデバッグ出力をすればいいかの提案だけして、そのコマンドの実行はしないでください。
- 一度の会話ターンに複数回コードの変更をしないでください。
- 基本的にはtrain_SemanticKITTI.pyを実行してもらうことになると思うのですが、実行コマンドはtcussというAnaconda環境をアクティベートしたうえで、次のフォーマットに従ってください。WANDB_MODE=disabled python train_SemanticKITTI.py --name debug --save_path data/users/minesawa/semantickitti/debug --workers 0 --cluster_workers 0 --batch_size 16 16 --select_num 128 --eval_select_num 16 --max_epoch 2 2 --cluster_interval 1

